//! the slice method will return a copy of the specified portion of the array.
//! the portion is specified by the index given in the parameters

const array = [10, "student", "cake", -78, "Zebra"];

console.log(array.slice(1, 3));
